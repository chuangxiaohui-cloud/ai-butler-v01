# v2.5 增补：IM 远程对话 + Skill 市场

> 基准：v2.5 定稿（tag `v2.5`）｜编制：2026-08-13｜更新：2026-08-14（§2.3/§2.6/§5 对齐 agent-skills 格式）｜状态：**已登记进 v2.5 §1.4（R-02/R-03/R-04），决策点待老张拍板，拍板后实施**
> 来源：老张 2026-08-13 提出的三个新需求（需求2 IM 远程对话 + 需求3 Skill 市场）
> 对齐：§4.4 MVP 切片 / §8.2 Skill 生命周期 / §10 安全模型 / §12 成熟度

---

## 0. 结论置顶（TL;DR）

| 需求 | 结论 | 版本落点 | 对 v0.1 的影响 |
|------|------|---------|---------------|
| **IM 远程对话** | 独立 IM Bridge 服务，飞书优先 | v1.1（v1.0 全功能后） | 零影响——Bridge 与主 Agent HTTP 解耦 |
| **Skill 市场安装** | §8.2 新增第三来源"市场安装" | v1.2（Skill 生命周期成熟后） | 零影响——v0.1 只做预置 2 项 |

**核心判断**：两个需求都不影响 v0.1 搜索管道开发，属于 v1.0 后增量。但架构预留现在就要做（HTTP API 接口 / Skill 安装接口），避免 v1.0 后大改。

---

## 1. 需求2：IM 远程对话

### 1.1 架构方案

```
┌─────────────┐     ┌──────────────────┐     ┌─────────────┐
│  IM 平台     │     │  IM Bridge       │     │  主 Agent    │
│  (飞书/微信/  │────▶│  (独立 Node 进程) │────▶│  (HTTP API)  │
│   QQ)        │◀────│                  │◀────│             │
└─────────────┘     └──────────────────┘     └─────────────┘
                     │  适配器层        │
                     │  - 飞书 Adapter  │
                     │  - 微信 Adapter  │
                     │  - QQ Adapter    │
                     └──────────────────┘
```

**设计原则**：
- **Bridge 独立进程**：与主 Agent 解耦，Bridge 挂了不影响桌面端，桌面端关了 Bridge 还能跑
- **HTTP API 通信**：Bridge → 主 Agent 走 `POST /api/chat`（复用 §6 搜索管道），不直连 LLM
- **适配器模式**：每个 IM 平台一个 Adapter，实现统一接口 `IMAdapter`
- **异步回复**：IM 平台有消息超时（飞书 15s、微信 5s），先回"正在思考…"，结果就绪后异步推送

### 1.2 统一消息接口

```typescript
// IM Bridge → 主 Agent
interface IMQuery {
  platform: 'feishu' | 'wechat' | 'qq';
  userId: string;       // IM 平台用户 ID（白名单校验）
  sessionId: string;    // IM 会话 ID（映射到 Agent 会话）
  text: string;         // 用户消息原文
  timestamp: number;
}

// 主 Agent → IM Bridge（异步回调）
interface IMResponse {
  sessionId: string;
  answer: string;       // Agent 回答
  confidence: number;   // §6 置信度
  evidence?: string[];  // §9 证据链（精简版，IM 不展示完整证据）
  gateTriggered?: boolean;  // §6.1.3 规则③安全阀
}
```

### 1.3 技术选型

| 平台 | 方案 | 优势 | 风险 | 优先级 |
|------|------|------|------|--------|
| **飞书** | 官方 OpenAPI + 自建机器人 | 最规范、文档全、无封号、支持富文本卡片 | 需创建飞书企业应用 | **P0**（第一个实现） |
| **企业微信** | 官方 API + 应用消息 | 有官方支持、比个人微信安全 | 需企业微信管理员权限 | P1 |
| **个人微信** | wechaty / 极客版 | 用户体验最自然 | 🔴 封号风险高、依赖第三方逆向 | P2（老张评估风险后决定） |
| **QQ** | NapCat / LagRange | 开源活跃、比 go-cqhttp 稳定 | 依赖第三方逆向、稳定性不确定 | P2 |

**推荐路线**：飞书先行 → 企业微信 → 个人微信/QQ（视需求）

**理由**：
1. 飞书有完整的 Bot API，支持主动推送消息（异步回复必需），不依赖逆向
2. 企业微信 API 规范，但需要企业微信账号
3. 个人微信 wechaty 方案有封号风险——老张的微信号如果被封影响太大，不推荐作为第一选择
4. QQ NapChat 方案可用但维护不稳定

### 1.4 安全考虑

| 安全维度 | 规则 | 对齐 v2.5 |
|---------|------|----------|
| **用户白名单** | IM Bridge 只接受白名单用户消息（老张的 IM ID），非白名单直接丢弃 | §10.1 权限模型 |
| **Token 存放** | IM 平台 API Token 存 `.env`，与 LLM API Key 同级 | §10.3 + .env |
| **消息脱敏** | IM 消息同样走 §10.3 脱敏规则，不传敏感信息给搜索引擎 | §10.3 |
| **权限降级** | IM 远程对话**只支持知识问答模式**，不支持项目协作（需桌面端操作） | §4.1 三栏交互 |
| **会话隔离** | IM 会话 ≠ 桌面端会话，通过 sessionId 映射，不串上下文 | §8.3 上下文分层 |
| **超时处理** | 先回"正在思考…"（<3s），结果就绪后异步推送 | [P-41] SDK 超时 |

### 1.5 版本落点

| 版本 | 范围 | 前置条件 |
|------|------|---------|
| v1.0 | 主 Agent HTTP API 就绪（`POST /api/chat` 对外可用） | v0.1-v1.0 搜索管道全量验收 |
| **v1.1** | IM Bridge 飞书版（白名单 + 异步回复 + 知识问答） | v1.0 + 飞书企业应用创建 |
| v1.2 | 企业微信 Adapter | v1.1 + 企业微信账号 |
| v1.3 | 个人微信/QQ Adapter（视老张需求） | v1.2 + 风险评估 |

**对 v2.5 文档的影响**：
- §4.1 新增"远程对话"交互模式（第四种？或作为知识问答栏的远程扩展）
- §10 新增"IM Bridge 安全规则"小节
- §13 新增 IM Bridge 代码目录
- §5 新增 PARAM（IM 超时阈值、白名单最大用户数等）

---

## 2. 需求3：Skill 市场安装

### 2.1 现状分析

v2.5 §8.2 Skill 生命周期当前只有**两条来源**：
1. **预置 Skill**（§12.2 清单 6 项，随安装打包）
2. **用户创建 Skill**（使用中提炼，👍 后升级为 L1）

缺少第三条来源：**市场安装**——从外部 Skill 市场发现、下载、安装 Skill。

### 2.2 架构方案

```
┌─────────────┐     ┌──────────────────┐     ┌─────────────┐
│  Skill 市场  │     │  Skill Manager    │     │  本地 skills/ │
│  (Git 仓库/  │────▶│  (主 Agent 内置)  │────▶│  目录        │
│   API 索引)  │     │  1. 搜索/浏览     │     │  SKILL.md    │
│              │     │  2. 下载+SHA-256  │     │  scripts/    │
│              │     │  3. 安装+注册     │     │  references/ │
│              │     │  4. 更新/卸载     │     └─────────────┘
└─────────────┘     └──────────────────┘
```

### 2.3 Skill 包格式（参考 WorkBuddy）

```
skill-name/
  SKILL.md          # 元数据 + 指令（必需）
  scripts/          # 可选脚本
  references/       # 可选参考资料
  assets/           # 可选资源（图片/模板等）
  skill.json        # 安装元数据（签名/版本/来源）
```

**SKILL.md frontmatter**：
```yaml
---
name: chip-analysis
version: 1.2.0
trigger: "intent:chip_analysis"
description: "芯片选型与避坑分析"
author: "老张"
permissions:
  - file_read   # 需要读文件
  - network     # 需要联网
  - command     # 需要执行命令
signature: "sha256:abc123..."  # 市场签名
---
```

**SKILL.md 内容结构**（兼容 agent-skills 格式，v1.2 解析器直接支持）：

| 段落 | 作用 |
|------|------|
| Overview + When to Use | 何时启用 |
| Process | 分步工作流（Step 1..N） |
| Red Flags | 反模式列表 |
| Verification | 证据要求（checkbox 清单） |
| Rationalizations | 反合理化表（跳步借口 + 反驳） |

> **参考**：addyosmani/agent-skills（GitHub, MIT）——24 技能 / 4 专家代理 / 7 清单，本地镜像 `agent-skills/`，可作为首个市场源试点（2026-08-14 老张拍板格式对齐）。

### 2.4 安装流程

```
用户浏览市场 → 选择 Skill → 下载到临时目录
→ SHA-256 校验（签名不符拒绝安装）
→ 权限审查（展示 Skill 声明的权限，用户确认）
→ 安装到 skills/ 目录
→ 自动注册四字段元数据到 §8.2 治理体系
→ 首次运行沙箱执行（§10 安全模型）
```

### 2.5 与 §8.2 Skill 生命周期集成

| §8.2 治理规则 | 市场安装 Skill 的处理 |
|--------------|---------------------|
| 四字段元数据 | 安装时自动填充（触发模式/版本/最近使用=安装时间/使用次数=0） |
| 冷存不删 [P-31] | 与预置/用户创建同等——闲置 90 天移出活跃库 |
| 衰减 [P-30] | 同等衰减规则 |
| 触发冲突按特异性优先 | 同等优先级规则 |
| 用户 👎 连续 [P-79] 次 | 同等降权规则 |
| 成熟度联动 | 市场安装的 Skill 计入 Skill 覆盖度指标 |

**关键差异**：市场安装的 Skill 首次运行需用户确认权限（预置 Skill 免确认），后续运行不再询问。

### 2.6 安全考虑

| 安全维度 | 规则 | 对齐 v2.5 |
|---------|------|----------|
| **来源验证** | 只从可信市场安装（白名单源：自建 Gitee 仓库 + 官方市场 + 已验证开源仓库，如 agent-skills[GitHub/MIT]） | §10.1 |
| **签名校验** | SHA-256 验证完整性，签名不符拒绝安装 | §10.4 安全 TDD |
| **权限分级** | Skill 声明 file_read/file_write/network/command 权限，用户确认后授权 | §10.2 命令白名单 |
| **沙箱执行** | Skill 在 §10 安全模型沙箱内运行，不可越界 | §10.1 文件沙箱 |
| **自动更新** | 默认关，用户手动触发更新（避免静默变更行为） | — |

### 2.7 版本落点

| 版本 | 范围 | 前置条件 |
|------|------|---------|
| v1.0 | §8.2 Skill 生命周期全量就绪（四字段/冷存/衰减/降权） | v0.1-v1.0 |
| **v1.2** | Skill 市场最小版：浏览/下载/安装/卸载（从自建 Gitee 仓库） | v1.0 + §8.2 成熟 |
| v1.3 | 权限分级 + 自动更新 + 官方市场对接 | v1.2 |

**对 v2.5 文档的影响**：
- §8.2 新增"8.2.3 市场安装"小节（第三来源）
- §10 新增"Skill 沙箱执行规则"小节
- §5 新增 PARAM（市场签名校验超时、安装最大体积等）
- §12.4 成熟度等级表新增"市场 Skill 覆盖度"指标

---

## 3. 对 v2.5 文档的变更影响汇总

| v2.5 章节 | 变更内容 | 触发版本 |
|----------|---------|---------|
| §4.1 三栏交互 | 新增"远程对话"交互模式说明 | v1.1 |
| §8.2 Skill 生命周期 | 新增 8.2.3 市场安装（第三来源） | v1.2 |
| §10 安全模型 | 新增 IM Bridge 安全规则 + Skill 沙箱执行规则 | v1.1/v1.2 |
| §12.4 成熟度 | 新增市场 Skill 覆盖度指标 | v1.2 |
| §13 代码目录 | 新增 IM Bridge + Skill Manager 目录 | v1.1/v1.2 |
| §5 PARAM | 新增 IM 超时/白名单/市场签名等参数 | v1.1/v1.2 |

**重要**：这些变更在 v1.0 全功能验收后才落地。v0.1-v0.2 阶段只需做架构预留：
1. 主 Agent HTTP API 接口设计预留 `/api/chat` 端点
2. Skill 安装接口设计预留 `SkillManager.install()` 方法签名

---

## 4. 架构预留（v0.1 即刻可做）

为避免 v1.0 后大改，v0.1 阶段在代码骨架中预留以下接口（不实现，只定义接口签名）：

### 4.1 HTTP API 预留

```typescript
// src/api/server.ts（v0.1 只定义接口，不实现）
// v1.1 IM Bridge 调用此端点
app.post('/api/chat', async (req, res) => {
  // v0.1: 返回 501 Not Implemented
  // v1.0: 调用 pipeline(query) 返回结果
  // v1.1: 支持异步回调（IM Bridge 用）
});
```

### 4.2 Skill Manager 预留

```typescript
// src/skills/manager.ts（v0.1 只定义接口，不实现）
interface SkillManager {
  list(): Skill[];                    // v0.1: 返回预置 Skill
  install(source: string): Promise<Skill>;  // v1.2: 从市场安装
  uninstall(name: string): void;      // v1.2: 卸载
  update(name: string): Promise<void>; // v1.2: 更新
}
```

---

## 5. 决策点（待老张拍板）

| # | 决策点 | 选项 | 推荐 |
|---|--------|------|------|
| ① | IM 平台优先级 | 飞书先行 / 企业微信先行 / 个人微信先行 | **飞书先行**（最规范、无封号风险） |
| ② | 个人微信是否做 | 做（接受风险）/ 不做 / 先观望 | **先观望**（wechaty 封号风险高，等 v1.2 再评估） |
| ③ | Skill 市场来源 | 自建 Gitee 仓库 / 对接 WorkBuddy 市场 / 两者都做 | **自建 Gitee 仓库**（可控、可定制） |
| ④ | 架构预留是否纳入 v0.1 | 是（即刻预留接口）/ 否（v1.0 再加） | **是**（成本低、避免大改） |
| ⑤ | 增补内容何时合并进 v2.5 | 即刻合并为 v2.6 / v1.0 验收后合并 | **v1.0 验收后合并**（避免提前膨胀） |
| ⑥ | 首个市场源试点 | 自建 Gitee 仓库 / agent-skills（GitHub, MIT）直接解析 / 两者都做 | **两者都做**（Gitee 装自有 Skill，agent-skills 装通用软件工程质量门控；格式对齐 2026-08-14 已拍板） |
