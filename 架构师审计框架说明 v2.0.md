# 架构师审计框架说明 v2.0

> 本文档定义本次审计的角色定位、思维模型与输出要求。
> 审计方请以【资深架构师】身份执行，而非 QA / 代码规范检查员。
> 
> ⚠️ 版本对齐声明：本框架配套 audit-package-checklist **v2.5** 使用。
> 所有 § 编号均指向 v2.5 清单，若发现编号不一致请以清单为准并立即反馈。
> 修订 v2.0.1（2026-08-30）：¥447 口径更正为「累计开发成本」（owner 确认：开发至今实际已用 447 元，非剩余预算）；§4.2 补回归成本归属；§3.7 DI 措辞与 Checklist §7 对齐。

## 1. 角色定位

你不是在"找 bug"，而是在回答一个问题：

> **"以累计 ¥447 开发成本约束和 AI 辅助开发模式，这个架构能否支撑一人公司 AI-Agent v2.5 核心场景稳定运行 6 个月以上？"**

所有检查项都服务于这个问题。

### 关键术语定义

| 术语 | 定义 | 判定依据 |
|------|------|----------|
| **¥447 开发成本** | 项目开发**至今的累计投入**（owner 口径：目前实际已用 447 元，截至 2026-08-30）——**不是**剩余修复预算、**不是**总项目预算。修复建议须评估成本相对累计投入的量级，量级不成比例的重构必须附带分阶段方案或量化证据证明"不修则 6 个月内必然重大故障" | 项目财务记录 |
| **核心场景** | 以下三类场景的交集，缺一不可：<br>① 需求文档 §3 用户画像中明确列出的高频任务<br>② 需求文档 §6 搜索问答主链路覆盖的功能点<br>③ 需求文档 §10 安全门保护的边界场景 | 需求文档 v2.5 §3 + §6 + §10 |
| **T+3** | T = **审计启动日**（即代码仓库只读权限开放之日）。T+3 = 启动日后第 3 个工作日结束时 | 委托确认邮件/消息时间戳 |

## 2. 思维模型：三层过滤

对每个发现，依次过三层过滤器：

| 层级 | 问题 | 通过条件 |
|------|------|----------|
| L1 业务影响 | 这个问题会导致**核心场景**（见 §1 定义）出现功能失效、数据错误、安全风险（含 Prompt Injection / SSRF / 命令注入）吗？ | 是 → 进入 L2 |
| L2 修复成本 | 在当前代码结构和 AI 辅助模式下，修复成本是否 ≤ 3 人天（AI 辅助估算口径）？ | 是 → 进入 L3；否 → 标记为"架构债务"，给出分阶段迁移方案 |
| L3 优先级 | 相比其他发现，这个问题的紧急程度排第几？ | TOP 10 → 写入报告正文；其余 → 附录 |

**未通过 L1 的发现**：不入报告正文，仅在附录"低优先级观察"中以一句话带过。

## 3. AI 项目特异性认知

本项目代码由 Codex + DeepSeek 生成，具有以下特征，审计时需特别注意：

1. **防御性过当**：AI 倾向到处加 try-catch + fallback，错误可能被静默吞掉
2. **同义反复测试**：测试可能只断言"代码做了代码做的事"，未验证业务意图
3. **过度抽象**：可能存在仅被调用一次的 interface/factory/strategy
4. **注释漂移**：AI 改代码时常忘更新注释，注释可能与实现不一致
5. **PARAM 脱节**：代码中的默认值/阈值可能与 PARAM 注册表声明不一致（→ Checklist §5.1）
6. **Mock 过度 / 集成盲区**：单元测试大量 mock 外部依赖，但集成测试未覆盖真实调用路径
7. **依赖注入真实性**：注入点可能从未被真实链路消费（装饰性 DI）（→ Checklist §7）

> 📌 完整 7 项特征及对应检查项见 **Checklist §7**。以上仅为摘要提示。
> 
> ⚠️ 章节引用更正：AI 代码特异性检查项位于 Checklist **§7**（非 §5-§6）；
> PARAM 脱节检查项位于 Checklist **§5.1**。

## 4. 必查架构契约

以下为项目硬约定，审计方**必须验证**，违反即视为 CRITICAL：

### 4.1 核心问答契约一致性

`answer(query) → {answer, confidence, evidence[], gate_triggered}` 的输出结构必须在以下三个入口**共用同一 pipeline**：

- CLI 命令行调用
- API Gateway HTTP 接口
- UI 前端组件

**禁止**任何入口另起独立问答链路。验证方法：追踪三个入口的代码路径，确认最终调用同一个 `answer()` 函数或等价封装。（依据：AGENTS.md 硬约定）

### 4.2 回归基准保护

所有修复建议**不得破坏** `bench:devil-v25` 122 条基准测试的通过率。

- 修复前：记录当前基准通过率
- 修复后：重新运行全部 122 条，通过率不得低于修复前
- 如修复导致基准失败，必须在报告中说明原因并提供补偿方案
- **回归成本归属**：优先引用 `bench/` 既有基线摘要 + 对受影响 query 定向回归；确需全量重跑 122 条时由 owner 侧承担（仓库成本纪律：devil-v25 耗时长且烧 token，审计方不代跑）

### 4.3 AI 安全面（L1 强制项）

以下安全问题一旦存在，无论修复成本多高，均直接通过 L1 进入 L2/L3：

- Prompt Injection（用户输入注入系统 prompt）
- SSRF（搜索/抓取功能被利用访问内网资源）
- 命令注入（shell/exec 调用未过滤用户输入）

审计方需具备对应测试能力（Checklist §0.1 已要求）。

## 5. 输出要求

### 5.1 报告结构
- 执行摘要（≤1 页）：结论 + TOP 3 风险 + 修复建议优先级
- 问题清单表格（格式见 §5.2）
- 详细分析（按模块组织）：证据 + 推理过程
- 附录：低优先级观察 / 工具限制说明 / 抽样清单 / bench:devil-v25 回归结果

### 5.2 问题清单表格模板

| 编号 | 模块 | 严重度 | 置信度 | 修复建议 | 预估工时 | **预估成本(¥)**（对照 ¥447 累计投入量级） | 回归影响 |
|------|------|--------|--------|----------|----------|-----------------|----------|
| R-xx | xxx | CRITICAL/HIGH/MEDIUM/LOW | HIGH/MEDIUM/LOW | 具体修改位置+内容 | x 人天 | ¥0~¥xxx | bench:devil-v25 通过/失败 x 条 |

> ⚠️ "预估成本(¥)"列为**必填项**，用于判断修复成本相对 ¥447 累计投入是否量级合理；无成本估算的发现视为不完整交付。

### 5.3 置信度标注（Checklist §9）

每项结论必须标注：
- **HIGH**：有自动化测试/静态分析/渗透测试证据
- **MEDIUM**：有人工代码审查 + 抽样验证
- **LOW**：仅基于文档/口头确认/有限观察

> ⚠️ 章节引用更正：置信度机制位于 Checklist **§9**（非 §8，§8 为已知缺口登记）。
> LOW 置信度项需在报告中单独汇总，注明验证局限与建议补验方式。

### 5.4 修复建议约束

- **原则上不提整体重写/更换框架**，除非同时满足：
  - 有量化证据证明当前架构在 6 个月内必然导致核心场景重大故障
  - 给出了分阶段迁移方案，且每阶段成本相对 ¥447 累计投入量级合理
- 每条建议需包含：具体修改位置 + 修改内容 + 预期效果 + 预估工时 + 预估成本(¥)
- 优先推荐"最小侵入式修复"（如加一行日志、改一个配置、补一条 PARAM 登记）
- 所有修复建议必须以"不破坏 bench:devil-v25 122 条基准"为前提

## 6. 沟通原则

- 遇到不确定的判断，标 LOW 并说明原因，不要硬给结论
- 工具不可用时，降级为人工方法并注明，不要跳过检查项
- 发现材料缺失时，主动声明"未发现"，不要自行脑补
- T+3 中期检查时提交阶段性成果，不要等到最终报告才暴露问题
- 发现本框架与 Checklist v2.5 编号/内容不一致时，**以 Checklist 为准**并立即反馈

## 7. 版本与配套

| 项目 | 版本 | 备注 |
|------|------|------|
| 本框架 | v2.0（v2.0.1 修订） | 2026-08-30 修订，吸收 Codex 7 项反馈；v2.0.1：¥447 口径更正为累计开发成本、回归成本归属、DI 措辞对齐 |
| 配套 Checklist | **v2.5** | 唯一有效版本，旧版作废 |
| 配套审计报告 | architecture-audit-2026-08-30.md | 交付前须确认报告中引用的 Checklist 版本为 v2.5、框架引用为 v2.0 |
| 回归基准 | bench:devil-v25 (122 条) | 修复验收口径 |