# 推进计划：👎 原因与文字补充

> 日期：2026-09-12 · 分支：v0.2b · 状态：已完成

## 目标

继续落实 §9.3：👎 提交前可选择规范原因标签并填写可选文字，沿用 E374 的 append-only 审计记录。

## 计划

1. 扩展反馈契约与存储字段
2. gateway 校验原因标签并拒绝非法组合
3. UI 增加 👎 原因面板与提交状态
4. 更新项目文档与当日交接

**验收标准**

- 支持“答非所问/太啰嗦/技术错误/漏了重点”四类原因和可选文字
- 👍 不携带负向详情，非法原因不落盘
- UI 只有提交成功后才关闭面板并点亮 👎
- 主项目、UI 构建与定向单测通过

## 执行过程

### 改动

- `AnswerFeedbackEntry` 新增可选 reason/note，四类原因使用固定枚举。
- gateway 校验反馈类型、栏位、原因与正负反馈组合，非法请求不落盘。
- UI 点击 👎 展开原因标签和可选文字面板；提交成功后才关闭并点亮。
- 👍 保持一步提交，且禁止携带负向原因或说明。

### 遇到的问题

- 无实现阻塞；沿用 E374 的 append-only 事件和最新值统计，未新增存储体系。

## 结果

- 验证：`npm run build`、`npm --prefix ui/prototype run build` 通过；`git diff --check` 无空白错误。
- 测试：feedback-store 1/1；gateway feedback API 1/1。
- 提交：未提交（遵循 owner 要求）。
- 遗留事项：“修改建议”按钮仍未接真实提交；doc-lint 仍由需求文档第 19 行既有 provisional 示例超期阻断。
