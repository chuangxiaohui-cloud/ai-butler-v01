# 推进计划：应用图标（E123）

> 日期：2026-08-16 · 分支：v0.2b · 状态：已完成

## 目标

去掉 Electron 默认图标，给桌面应用一个本地生成的应用图标：深色圆角底 + 青蓝渐变
聊天气泡 + 三圆点，符合“一人公司 AI-Agent”贴身秘书的定位；把图标接入
electron-builder 并重新出包。

## 计划

1. 新增 `desktop/scripts/make-icon.ps1`，用 System.Drawing 生成 512×512 PNG 到
   `desktop/build/icon.png`。
2. `desktop/package.json` 的 `win` 配置加 `icon: build/icon.png`。
3. 重新执行 `npm run dist`，确认打包日志不再提示“default Electron icon”。
4. 更新需求文档附录 A（E123）与交接记录，提交推送。

**验收标准**

- `desktop/build/icon.png` 存在且为 512×512。
- electron-builder 打包日志使用自定义图标。
- 安装版与便携版重新产出。

## 结果

- `desktop/build/icon.png`（512×512）、`icon-256.png`、`icon.ico` 已生成并提交。
- `desktop/package.json` 的 `win.icon` 指向 `build/icon.ico`；重新执行 `npm run dist`，
  打包日志不再出现 “default Electron icon”，NSIS 与便携版重新产出。
- 打包后 exe 的 smoke 实测通过（退出码 0，`DESKTOP_READY`）。
- 提交：E123 已提交并推送 Gitee/GitHub。
- 遗留：代码签名与 Tauri 瘦身迁移。
