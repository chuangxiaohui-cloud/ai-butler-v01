# 推进计划：v1.0 切片（首切片 = 深度报告）

> 日期：2026-08-23 · 分支：v0.2b · 状态：S1-S6 已收口，S7-S8 排期中

## 目标

v0.2b 里程碑已收口（E206），下一里程碑 v1.0 范围过大（§4.2：MCP 子 Agent + 证据链 UI + 安全模型 + 远程对话通道 + 代码托管联动 + Skill 市场 + 深度报告 + LLM 增强路由等留待项），按切片推进：先拆片，再落地首切片「深度报告」（§4.3.2 长任务首实例，[P-13] 一直无实测）。

## 计划

1. **切片分解**：S1 深度报告（长任务首实例）；S2 证据链 UI；S3 MCP 子 Agent；S4 安全模型补齐（全量）；S5 远程对话通道；S6 代码托管联动；S7 Skill 市场远程化；S8 LLM 增强路由 + fast description + MemoryCoreStore 切换（各留待项）。
2. **首切片 S1 = 深度报告**：路由新增 `deep_report` 意图（actionType + 规则表）；pipeline 深度报告路径（搜索证据 → 分阶段生成：大纲 → 分节 → 证据附录，[P-13] 13s 总预算，进度事件透出，AbortSignal 取消）；无 LLM/失败降级为规则组装（证据结构化报告）。
3. **登记**：params.ts 落 [P-13] `deepReportBudgetMs=13s`（C8 引用生效）；需求文档 E220；目录文档更新。
4. **验收**：doc-lint 0 FAIL 0 WARN + 全量单测/集成全绿。

**验收标准**

- `deep_report` 意图命中（"写一份…报告/调研报告/深度分析"等），规则表可路由。
- 深度报告含：结构化报告正文 + 证据附录；进度事件（outline/section-N/evidence）透出；超预算或失败降级不阻塞；取消信号生效。
- 无 LLM 时仍产出结构化报告（降级路径）。
- doc-lint 0 FAIL 0 WARN；单测 + 集成全绿。
## 切片进度

| 切片 | 内容 | E-NN | 状态 |
|------|------|------|------|
| S1 | 深度报告（长任务首实例，[P-13]） | E220 | ✅ `09e89bc` |
| S2 | 证据链 UI + 深度报告取消/恢复 | E221 | ✅ `fdf2392` |
| S3 | MCP 子 Agent 骨架 | E222 | ✅ `2f0fd42` |
| S4 | 安全模型补齐（§10.1-10.5） | E223 | ✅ `31ce84b` |
| S5 | 远程对话通道骨架 | E224 | ✅ `c18cbd6` |
| S6 | 代码托管联动（GitHub/Gitee 库级骨架） | E225 | ✅ `fcac8de` |
| S7 | Skill 市场远程化 | — | ⏳ 排期中 |
| S8 | LLM 增强路由 + fast description + MemoryCoreStore 切换 | — | ⏳ 排期中 |

## 执行过程

### 改动

- `src/agent/intent-feature.ts`：ACTION_TYPES 增 `deep_report` + ACTION_RE 检测（置于 create 前防抢）。
- `src/agent/routing-table.ts`：新规则 → `intent: 'deep_report'`（searchNeed=true）。
- `src/search/deep-report.ts`（新增）：分阶段生成（大纲/分节/证据附录），共享 [P-13] 预算，取消信号，无 LLM 降级。
- `src/search/pipeline.ts`：deep_report 路径接入（搜索证据 → 报告生成 → 证据附录），`PipelineOptions.signal` 透传。
- `src/config/params.ts`：[P-13] `deepReportBudgetMs=13_000`。
- 测试：intent 检测 / 规则命中 / deep-report 模块（LLM 路径、预算超时降级、无 LLM 降级、取消）/ pipeline 分发。
- 文档：`docs/plans/2026-08-23-v1-slicing.md`（本计划）、需求文档 E220、`docs/code-directory.md`、`docs/directory-structure.md`、handoff。

### 遇到的问题

- 分阶段「取消/恢复」：首切片实现取消（AbortSignal），恢复需状态持久化，留待 S2 后补（诚实登记）。

## 结果

- 验证：`npm exec tsx scripts/doc-lint.ts` 0 FAIL 0 WARN（C8 36 key、附录 516/950）；`npm run build` + `npm run test:all` 全绿。
- 测试：单测 689/690（1 skip）+ 集成 15/15。
- 提交：`09e89bc`（E220）+ `4de1081`（handoff 登记）· 推送：Gitee / GitHub
- 遗留事项：S1 取消/恢复已收口（S2）；[P-13] 保持 provisional，待真实使用实测后按 E197 口径复测；S6 代码托管库级骨架已收口（`docs/plans/2026-08-23-v1-s6-code-hosting.md`），真实仓库推送由 `push:hosts` 运维通道承载；剩余 S7/S8 按排期推进。
