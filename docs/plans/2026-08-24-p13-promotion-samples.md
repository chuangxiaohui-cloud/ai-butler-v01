# 推进计划：[P-13] 晋升样本（E232）

> 日期：2026-08-24 · 分支：v0.2b · 状态：已完成

## 目标

接 E231 遗留：补 n≥15 真实 LLM 样本，按 E197 复验门评估 [P-13]=45s 晋升定稿或再校准
（约束 P-15+P-13<=P-14=59s 内，超时率 ≤10%，新值 ≈ p95×1.2）。

## 计划

1. 真跑 `npm run deep:bench -- --samples 15`（LLM 模式，deepseek heavy，需网络 + token）。
2. 汇总 min/p50/p90/max、超预算占比、llm/fallback/timedOut 计数。
3. 按 E197 复验门判定：超时率 ≤10% 且 p95×1.2 ≤ 59s-14s=45s → 晋升 45s 定稿；否则再校准。
4. 变更落地：§5 注册表 [P-13] 状态、附录 A E232（bench:B-20260824-05）、handoff、计划结果段。
5. 验证：doc-lint 0 FAIL 0 WARN；build + test:all 全绿。

**验收标准**

- n≥15 真实样本记录在案（bench 输出 + 摘要登记）
- 晋升/维持决策有明确数据依据（超时率、p95）
- doc-lint 0 FAIL 0 WARN（附录行数预算内）

## 执行过程

### 改动

- `bench/B-20260824-05-p13-promotion-samples.md`：n=15 逐条耗时表 + 汇总分位 + E197 门逐项评估。
- 需求文档附录 A：登记 E232（数据包，无参数值/状态变更）。
- `docs/2026-08-24-progress-handoff.md`：今日收口补 E232 行。

### 遇到的问题

- 45s 预算下 1/15 超预算（#6=45004ms 超 4ms、0.009%）：source=llm、sections=3 完整，
  判定为计时边界抖动（末节在预算截止瞬间完成），非结构性不足。
- p95×1.2≈53.7s 会击穿 P-14 约束（14+53.7=67.7>59s）→ 不能按公式上调；
  维持 45s（约束内最大值）超时率 6.7%≤10% 已达标。

## 结果

- 验证：`npm run deep:bench -- --samples 15` 真跑 15 轮 **15/15 source=llm、0 fallback**
  （34718/38224/36664/38274/39031/39314/40644/41037/41072/42222/42273/42654/43718/44788/45004ms）
  doc-lint 0 FAIL 0 WARN（附录 528/950）。
- 测试：本批次无代码改动（纯文档 + bench 证据），无单测/集成变更。
- 提交：`c0951f3`（E232 数据包）+ `77f6347`（handoff 登记）+ 本提交（计划结果段）
- 遗留事项：[P-13]=45s 维持 provisional@2026-08-24；owner 签认 E232 数据包（复验门⑤）
  后登记 E233 正式转定稿（推荐维持 45s；严格 p95×1.2=54s 需连带 P-14→68s）。

