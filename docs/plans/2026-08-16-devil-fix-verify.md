# 魔鬼训练 v2.5 最小修复包验证（修复前后对比）

> 日期：2026-08-16 | 对比：git HEAD 旧基线 vs 修复后全量重跑（122 条）
> 对比脚本：`scripts/compare-devil-baseline.ts`（`npm run compare:devil-v25`）

## 1. 总体结果（原始基线 → 第二轮最终）

| 故障形态（122 条全量） | 修复前 | 修复后 |
|---|---:|---:|
| 路由选项/排期 | 25 | 0 |
| JSON 泄漏 | 7 | 0 |
| 低置信兜底话术 | 9 | 17 |

35 条系统级 Bug：**35/35 已修复**（7/7 JSON、4/4 安全、24 条路由全部转正）。
8 条能力项：**5/8 有进展**（ET05/ET06/ET14/ET30/EC23 从兜底变真实回答）。

说明：低置信兜底话术从 9 涨到 17，是因为路由修好后更多问题真正走了搜索，但搜索质量/配额波动导致一部分仍落到“我暂时无法确认”；这是下一阶段重点，不是路由问题。

## 2. 已转正代表

- ET04/ET20/ET28、SM03/SM04/SM05/SM11/SM13/SM29、EC07/EC19/EC21、P10：从“排期选项”变成带证据的工程回答。
- SM01/SM09/SM18/SM19/E40：从 `missing_time` / `send_message` JSON 变成真实搜索回答。
- EC04/EC12/EC13：从“人身急救模板”变成合规拒绝（gate=safety）。
- EC26：从“溺水救援模板”变成手机进水止损步骤。
- ET05/ET14/ET30：从“我暂时无法确认”变成有内容的排查/建议。
- ET06/SM07：对比类问题从澄清/报价误路由变成真实搜索回答。
- EC06：周末股市问题直接回答“休市”。
- EC23/EC30/P05：从兜底/澄清变成真实回答。
- EC11/EC22：缺信息画图/综述改为主动澄清“补充具体信息”。

## 3. 仍待修（诚实清单）

### 3.1 仍低置信兜底/搜索失败（主要剩余）

- SM18/SM31/C06/C08/ET20/P03 等仍返回“我暂时无法确认”：路由已正确，是搜索质量/配额/二次取证问题。
- 方向：强化浏览器兜底、重试与官方源子查询；必要时对高频失败题单独配查询改写。

### 3.2 已收尾项（今日最后一批）

- C02/E39：LLM 重模型默认超时 8s → 30s，engineer 执行器恢复真实代码生成（I2C 驱动、位置式 PID 均实测通过）。
- C05：新增 `project-packager` Skill（排除 .git/node_modules/build 后 PowerShell 压缩），无路径时先澄清目录。
- C06：`github-reader` 从占位转真，抓 README/仓库信息（raw.githubusercontent 不可达时给仓库链接兜底）。
- P03：天气+芯片多意图在无证据时明确拆分引导（天气需城市、芯片库存可继续查）。
- s1：明确动作指令（打包/重写/改/画/写等）不再被“指代澄清”拦截。

### 3.2 执行器运行时问题

- C02（PID 修正）已路由到 engineer 执行，但运行时返回“代码生成失败，可以稍后重试”，需查 LLM 执行器链路。
- E39（I2C 驱动）同属执行器链路，需确认 engineer skill 与 LLM 接入稳定性。

### 3.3 多意图/专用能力

- P03（天气 + 芯片库存）仍需多意图拆解。
- C05（项目打包）已路由 pack_project 澄清，但真正的压缩执行链路待建。
- C06（GitHub 分析）已走 web_search，专门的 github-reader Skill 仍是占位。

## 4. 结论

两轮修复后：JSON 泄漏归零、安全误匹配归零、路由误判从 25 降到 0，35 条系统级 Bug 全部消除。剩余问题从“路由/安全”转移到“搜索质量 + 执行器运行时 + 多意图”，这些属于工程稳定性与功能完整度，不是模型能力天花板。
