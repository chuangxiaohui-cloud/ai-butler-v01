# 推进计划：日程标题清洗剥离「提前 N 分钟提醒」从句并清尾部标点（E329）

> 日期：2026-09-04 · 分支：v0.2b · 状态：已完成
> 承接：owner「继续推进」；E325/E327 标题清洗系列边角料——带提前量提醒的建日程会把提醒从句残留进事件名。

## 目标

标题不再残留「，提前 10 分钟提醒」类从句与尾部标点。
示例：`帮我安排明天下午3点的周会，提前10分钟提醒` → 标题 `下午3点周会`（提醒仍由 parseLeadMs 从原 query 正常登记）。

## 改动

- `src/skills/calendar-skill/index.ts`：把标题清洗抽成可测纯函数 `cleanCalendarTitle()`（create 路径内联逻辑迁移，导出）；在 E325/E327 清洗基础上追加剥离 `「，提前…提醒」` 从句（`/[，,]\s*提前[^，。；]*提醒/g`）并清尾部标点/空白。
- `src/skills/calendar-skill/index.test.ts`：新增 1 条纯函数用例（剥离提前提醒从句 / E327 例保持 / 无提前量句不受影响）。

## 结果

- 验证：`npm run build` 绿；calendar-skill 定向 16/16；`npm run doc-lint` 0 FAIL 0 WARN；全程零外部 LLM/API（¥0）。全量 `test:all`/bench 未跑（成本纪律）。
- 文档：需求文档附录 A E329 登记；本计划；09-04 交接追加轮。
- 提交：未提交（延续工作区待统一确认批次）。
