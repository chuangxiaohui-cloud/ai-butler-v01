# 推进计划：A/B 套评测拆分

> 日期：2026-08-18 · 分支：v0.2b · 状态：已完成

## 目标

把 122 条魔鬼训练拆成 A/B 两套：A 套可单发评测，B 套记忆/上下文/多轮依赖题单独评测，
避免单发评分系统性地误伤带记忆条目。

## 计划

1. 按历史审阅口径整理 B 套名单：EC02/EC10/EC24/EC29、P02/P04/P07/P08/P10、
   C01/C02/C03/C05/C07。
2. 新增 `scripts/split-devil-ab.ts`，输出 CSV、JSON 集合与说明文档。
3. `bench-devil-v25.ts` 支持 `--set=a|b`，子集报告独立命名，不覆盖全量报告。
4. 登记 E129，更新交接与测试策略。

**验收标准**

- A+B = 122，无重复、无遗漏。
- `npm run bench:devil-v25 -- --set=a` / `--set=b` 生成独立报告。
- 全量门禁通过。

## 执行过程

### 改动

- 新增 `scripts/split-devil-ab.ts`。
- 修改 `scripts/bench-devil-v25.ts`：`--set` 筛选 + `report-a/b.md` 等独立产物。
- 新增 `bench/devil-v25/ab-split.md`、`ab-split.csv`、`ab-sets.json`。

## 结果

- A=108，B=14，总数 122。
- A 套平均参考分 1.84；B 套平均参考分 0.86，确认记忆/上下文依赖题单发仍弱，正是后续
  多轮评测对象。
- 验证：两套报告生成成功；全量单测/集成与 doc-lint 通过。
