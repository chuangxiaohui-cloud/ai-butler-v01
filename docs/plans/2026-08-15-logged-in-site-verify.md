# 推进计划：需登录站点完整抓取链路验证

> 日期：2026-08-15 · 分支：v0.2b · 状态：已完成

## 目标

验证 QQ 浏览器 CDP 登录态在真实“需登录”页面上生效：Agent 新进程自动连 9222 并抓取立创商城会员中心，能识别当前是否已登录（只输出状态判断，不展示个人数据）。

## 计划

1. `browser:status` 确认 QQ 9222 仍在监听。
2. `browser:fetch` 抓取 `https://i.szlcsc.com/`（会员中心/登录页），用脚本只输出“已登录/未登录”判断。
3. 更新进度与计划文档，提交推送。

**验收标准**

- 新进程能自动连 QQ CDP 并抓取页面。
- 能给出登录态判断且不输出个人敏感内容。

## 执行过程

### 改动

- 无代码改动；本次为真实链路验证。

### 遇到的问题

- 立创会员中心当前未登录，抓取后跳转到嘉立创统一登录页（`passport.jlc.com/login?redirectUrl=...member.szlcsc.com...`）；说明登录态复用的前提是用户在 QQ 浏览器里已登录对应站点。

## 结果

- 验证：`browser:status` 确认 9222 与会话域 `member.szlcsc.com`；新进程 `browser:fetch https://member.szlcsc.com/` 自动连 QQ CDP 并正确跟随跳转到登录中心，链路机制生效。
- 登录态结论：当前 QQ profile 未登录立创会员中心；用户在该站登录一次后，Agent 即可复用该会话。
- 测试：无代码改动；doc-lint 0 FAIL/0 WARN。
- 提交：`8072bda` · 推送：Gitee / GitHub
- 遗留事项：用户登录立创/芯查查后可再跑一次，确认可见个人订单/优惠等登录后内容。
