# 实现计划 2026-08-27：doc-lint isInDetailsBlock 盲点修复 + C1 附录 A 台账豁免（E253）

## 背景

doc-lint 的 `isInDetailsBlock` 向上扫描时把 §0 顶部行内代码（反引号）中的字面量 `<details>` 当作真实标记，导致几乎所有章节被判入 `<details>` 块，C1 数值扫描自该示例引入后静默失效（0 FAIL 为假绿）。

## 计划

1. 修复 `isInDetailsBlock`：逐行剥离反引号代码段后再计数 `<details>`/`</details>`。
2. C1 恢复执法后评估积压违规，按台账本质豁免附录 A（仅 C1；C2/C4/C7 仍扫描附录 A）。
3. 清理正文真实违规（§6 两处、§9 代码围栏 mock）。
4. 同步 §0.1 规则文字 + 登记附录 A E253。

## 执行

1. `isInDetailsBlock` 修复（doc-lint.ts）：`lines[i].replace(/`[^`\n]*`/g, "")` 后再匹配 `<details>`/`</details>` 标记。
2. 修复后同文档扫描：401 FAIL（附录 A 398 + §6 3）+ 1 WARN（§9 代码围栏 92%）——证实 C1 此前假绿。
3. checkC1 增附录 A 豁免（实测数值属台账本质，与附录 C 同理；全局豁免不动，C2/C4/C7 仍扫附录 A）。
4. §6 清理：E17「首条命中率 0%」→「首条均未命中」；news/how_to「0.5/0.4」→「一半/四成」。
5. §9 代码围栏 UI mock「置信度 92%」→「置信度 92」（围栏内 WARN 清零）。
6. §0.1 C1 规则文字同步「附录 A 台账豁免」；附录 A 登记 E253。

## 结果

- doc-lint 0 FAIL 0 WARN（C1 恢复真实执法：正文新增裸数值会被拦下；附录 A 台账数值豁免）。
- src 零改动，单测 940/941 + 集成 32/32 不受影响（本批为工具链 + 文档措辞清理）。
