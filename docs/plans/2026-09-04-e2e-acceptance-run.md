# 推进计划：E318–E334 整批手动 e2e 验收执行清单

> 日期：2026-09-04 · 分支：v0.2b · 状态：进行中
> 承接：owner「推进下一轮」——handoff 后续轮候选「真实 CLI/桌面 e2e 验收（含 E328 目录监听、E331 通知角标/分页冒烟）」；
> 目的：把 E318–E333 未提交批次 + E334（confirm 挂起风险/成本展示）按“验收→提交”铺一条可勾选路径（提交仍待 owner 拍板）。

## 准备

1. `npm run build` 绿 + `npm run doc-lint` 0 FAIL 0 WARN（当前已满足，复跑可选）。
2. 终端 1：`npm run gateway`，等监听 `http://127.0.0.1:8787`。
3. 浏览器开 **`http://127.0.0.1:8787`**（gateway 托管 UI 静态页，与 API 同源）。注意：勿用 `http://127.0.0.1:5173`（Vite dev 与 8787 跨域，gateway 未配 CORS，请求会被浏览器拦截——2026-09-04 B 组复验踩到）。

## A 组：日程 / confirm 真阻断链路（E324–E330、E334）

### A1 亲子游句（E325/E326/E327）
- 聊天输入：`帮我安排一下我家里明天的亲子游行程安排`
- 预期：确认文案人称第二人称（“你让我‘帮你安排一下你家里明天的亲子游行程安排’”）；批准后回执
  `✅ 老板，已按你的批准执行。已为你创建日程：亲子游行程（…）`；标题无“一下/我家里”。

### A2 周会提前提醒句（E329/E330）
- 聊天输入：`帮我安排明天下午3点的周会，提前10分钟提醒`
- 预期：出现「⏸ 待你裁决/执行」确认卡（非“本地兜底”）；批准后创建的是**日历事件**（标题「下午3点周会」，
  无“提前…提醒”残留），**不是**一次性提醒；执行回执不再拼 `> ai-butler…` / `M ../../../…` 的 git 噪音。
- 补充：裁决面板（右栏「裁决」）对同一句批准，也应得到同样的日历事件回执（E324 第二刀）。

### A3 否决与重复挂起（E324）
- 输入一句写类请求后选「取消/否决」→ 回「已取消该操作，不会执行」，`data/decision-log.jsonl` 追加一行否决事件；
- 同一会话已有待批准项时再发写类请求 → 提示先处理上一条，不重复挂起。

### A4 挂起文案风险等级与预估成本（E334）✅ 已通过
- 聊天输入（复用 A1/A2 任一写类句，如 `帮我安排明天下午3点的周会，提前10分钟提醒`），观察确认卡/挂起文案文本：
- 预期：在「⏸ 你让我…不会擅自执行。」之后出现一行
  `风险等级：低 ｜ 本次操作预估成本：¥0.00（本地确定性执行，无外部模型调用）`（calendar_skill 属本地确定性执行）；
  裁决面板同一条 pending 的 question 文本含相同风险/成本行。
- 补充（可选，视路由可达性）：若某句能命中内容生成类写操作（如 office_daily 邮件句），预期显示
  `风险等级：高 ｜ 本次操作预估成本：≤ ¥0.03（单次内容生成上界，按 /cost 单价估算）`；路由不可达则登记 skip。

## B 组：UI 旁路能力（E328/E331/E332/E333）

### B1 目录监听（E328）✅ 已通过
- 用记事本在 `M:\202608111\projects\` 下新建/修改/删除一个文件并保存
- 预期：右栏「文件」面板约 2s 内自动刷新（无需手动点刷新）；`~$`、`.tmp` 等临时文件不触发（语义=不会让面板自行刷新；`/api/files` 列表本身含临时文件属正常，手动刷新可出现）。

### B2 通知角标与分页（E331）✅ 已通过
- 在对话触发一条写类挂起 → 右栏非「通知」页时“通知”tab 出现红色未读数字；进「通知」页角标清零；
- 通知超过 20 条时显示「共 N 条 · 第 x/y 页」，可上一页/下一页。

### B3 离线兜底与重试（E332/E333）✅ 已通过
- `Ctrl+C` 停掉 gateway → 聊天任意提问 → 出现 `⚠️ 网关未连接…（本地预览，非真实回答）`，无假证据；
- 重开 gateway → 兜底按钮应显示 `↻ 重试「{原句}」`（原句预览），点击后收到真实回复且不重复用户气泡。

## C 组：CLI / 桌面冒烟

- C1 身份问答（免外部网络）：`npm run dev -- "你现在是什么模型"` → stdout 结构化 JSON，answer 含 `AI-Agent` 与模型 label。
- C2 桌面壳：`npm run desktop` 起壳 + `npm run desktop:smoke` 冒烟；无 GUI 环境则登记 skip。
- C3 gateway 读 API：`Invoke-WebRequest http://127.0.0.1:8787/api/decisions` → 200 且 `open` 为数组（可空）。

## 结果记录

- A4（E334，2026-09-04 21:5x，owner 复验）✅：聊天输入 `帮我安排明天下午3点的周会，提前10分钟提醒`（裁决旧 pending 否决清队后重发），挂起文案在「我不会擅自执行。」后带
  `风险等级：低 ｜ 本次操作预估成本：¥0.00（本地确定性执行，无外部模型调用）`；裁决卡片同文案。点批准后回执「✅ 老板，已按你的批准执行。已为你创建日程：下午3点周会（…2026-09-05T07:00:00.000Z）；已同步设置提醒（提前 10 分钟，2026/9/5 14:50:00）」——建的是日历事件非一次性提醒、无 git 噪音。
- A4 附注：复验中先撞到同会话 20:25 旧 pending（E334 上线前文案无风险/成本行）触发 A3「还有一步待批准」守卫，否决旧项后重发即通过——行为符合预期。A4 可选分支（office_daily 邮件句 `高 ｜ ≤ ¥0.03`）未验证，登记 skip。
- B1（E328，2026-09-04 夜，owner 复验）✅：`projects/` 目录（先手动新建）下 `a.txt` 新建/修改/删除均约 1–2s 自动刷新文件面板；`~$a.txt` 新建/删除均不触发自动刷新（语义达标，服务端实测两条 files_changed 均由真实文件变更产生）。
- B1 已知项 → E335 已收口（2026-09-04，owner 复验后改选「1」）：原 `/api/files` 列表不过滤 `~$`/`.tmp` 临时文件（新建后列表会显示，删除后残留至下一次真实刷新）；E335 已让列表侧按与 watcher 同口径剔除（见 `docs/plans/2026-09-04-file-temp-filter.md`）。
- B1 附注·E335（2026-09-04，owner 复验）✅：`projects/` 下新建 `~$a.txt` 后文件面板不再显示（owner 确认「不显示」），临时文件过滤达标。
- B2（E331）✅：写类挂起触发后「通知」tab 出现未读角标、进入通知页清零；通知 >20 条分页可翻页（owner 确认通过）。
- B3（E332/E333）✅：停 gateway 提问 →「⚠️ 网关未连接…本地预览」且无假证据、按钮带原句；重启 gateway 后点重试收到真实回复且不重复用户气泡（owner 确认通过）。
- 环境备注：B 组在 `http://127.0.0.1:8787` 同源页完成；`5173`（Vite dev）因 gateway 未配 CORS 不可用。
- C1 ✅（2026-09-04 夜，自动执行）：`npm run dev -- "你现在是什么模型"` → 结构化 JSON，answer 含「一人公司 AI-Agent」与 `deepseek-v4-flash`（medium 档），`elapsed_ms 63` 内置规则秒回、零模型消耗。
- C2 ✅（2026-09-04 夜，自动执行，前置停掉独立 gateway 后重跑）：`npm run desktop:smoke` → 内嵌 gateway 正常监听 8787 + `DESKTOP_READY`，exit 0；GPU context 报错为虚拟化环境良性噪音，不影响判定。
- C3 ✅（2026-09-04 夜，自动执行）：`GET /api/decisions` → 200 且 `open` 为数组（含 1 条历史 cli 会话 pending，与本次验收无关）。
每项勾选并写备注；失败项贴网关终端输出 / `data/trajectory.jsonl` 相关行。全部通过后 handoff 登记
“E318–E334 验收通过”，再进入统一提交批次决策。
