# 推进计划：UI v2 外壳 + mode/submode 契约（E109）

> 日期：2026-08-16 · 分支：v0.2b · 状态：已完成

## 目标

按《AI-Agent-v2.5_3》把 UI 原型重构为 IDE 风格外壳：L0/L1 侧边栏、右侧产物栏、
终端抽屉、设置双栏、对话区顶部胶囊模式标签；同时让 `/api/ask` 返回 `mode/submode`
驱动前端意图自动识别。

## 计划

1. 后端：新增 `src/agent/mode-mapper.ts`，五主镜片 → 三模式/子模式；
   `AnswerResult` 附加可选 `mode/submode`，pipeline 各分支返回。
2. UI：重写 `App.tsx` 为 L0/L1 + 中间对话/设置 + 右侧栏 + 底部状态栏 + 终端抽屉；
   模式胶囊标签 + Overlay 手动覆盖（锁定本会话）；Ask/Craft/Plan 移出 UI。
3. 设置面板六个菜单（服务商/安全/路由校准/技能/记忆/Token 用量）先做 mock 表单；
   终端受 Shell 权限控制。
4. 补 mode-mapper 与 gateway mode 断言；跑 build/test/doc-lint；
   登记需求文档（§6.3 / §13 / 附录 A E109），更新交接，提交推送。

**验收标准**

- `/api/ask` 返回 mode/submode，四字段契约不变。
- UI 构建通过，L0/L1/右侧栏/设置/终端可交互，模式标签可手动覆盖并锁定。
- 全量测试与 doc-lint 全绿。

## 执行过程

### 改动

- 新增 `src/agent/mode-mapper.ts` 与 `mode-mapper.test.ts`。
- `src/search/pipeline.ts`：`AnswerResult` 附加 `mode/submode`，route 后各分支带出。
- 重写 `ui/prototype/src/App.tsx`（L0/L1、右侧栏、设置双栏、终端抽屉、模式胶囊）。
- `ui/prototype/src/styles.css`：新增 shell-v2 全套样式与移动端折叠。
- `src/gateway/app.test.ts`：断言 `/api/ask` 返回 `mode=knowledge`。

### 遇到的问题

- JSX 中 `>_ Terminal` 需要转义为表达式。
- 旧 `toggleL1` 表达式类型冲突，简化为同图标切换/异图标展开。

## 结果

- 验证：主项目与 UI `npm run build` 通过；`npm run test:all` 单测 321/321 +
  集成 17/17 全绿；doc-lint 0 FAIL / 0 WARN。
- 提交：E109 已提交并推送 Gitee/GitHub。
- 遗留：设置面板写操作接真实 API（Provider 测试/持久化、路由校准导出、
  Token 计量）；artifact 事件流与右侧文件列表联动；终端真实执行通道。
