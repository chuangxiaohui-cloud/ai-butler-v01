# 进度交接 2026-08-24（v0.2b 续作）

> 当前分支：`v0.2b`｜E228（Tavily [P-64] 口径复算）、E229/E230（[P-13] 复测工具+初步实测）、
> E231（分节并行+预算校准）、E232（[P-13] 晋升样本数据包）、E233（[P-13] 晋升定稿）已提交。
> 上一份交接见 `docs/2026-08-23-progress-handoff.md`。

## 今日已收口

1. **Tavily [P-64] 口径复算（E228）**：新增 `src/search/tavily-usage.ts`（远端 `GET /usage` 权威快照，
   Bearer 认证、3s 超时、失败静默）；`tavily:smoke` 配额段升级为远端 vs 本地尝试次数对比；
   口径决策：本地计数=观察指标、远端=超额判定（`limit:null` 不做 `remaining=limit-usage`）；
   [P-64]=1000 数值不变，§5/§6.2.1 补注。附录 A E228（bench:B-20260824-01）。
2. **深度报告 [P-13] 复测工具（E229）**：`scripts/deep-report-bench.ts` + `npm run deep:bench`
   （dry-run 离线自检 / LLM 真跑，`--samples`/`--budget-ms` 可调）。附录 A E229（bench:B-20260824-02）。
3. **[P-13] 初步实测（E230）**：真跑 3 轮 13s 预算 3/3 fallback、2/3 超时；单次 heavy 8.5-9.4s、
   顺序 4 调用结构性需 ~34-38s。附录 A E230（bench:B-20260824-03）。
4. **[P-13] 分节并行 + 预算校准（E231，B 方案落地）**：`deep-report.ts` Stage B 并行
   （大纲 1 次 + 分节 1 次 = 2 轮 RTT）；**根因修复**——分节此前被 P-116 per-call 12s 预算截断
   （单节实测 25-29s），新增 `createDeepReportHeavyClient()`（per-call=[P-13]）并在 pipeline 接线；
   参数校准 [P-13] 13s→**45s**、[P-14] 27s→**59s**（14+45=59 约束保持，实测 31.0-39.0s 后按 ≈p95×1.2）；
   flash 对比有 27s 离群 → 维持重模型。真跑 **3/3 source=llm、0 timedOut**。附录 A E231
   （bench:B-20260824-04）。新增单测 3 条；全量单测 791/792 + 集成 15/15；doc-lint 0 FAIL 0 WARN。
5. **[P-13] 晋升样本数据包（E232）**：真跑 `npm run deep:bench -- --samples 15` 补 n=15 真实样本
   ——**15/15 source=llm、0 fallback**，min=34718 / p50=41037 / p90=44788 / max=45004 / avg=40642ms；
   超 45s 预算 1/15（#6=45004ms 超 4ms、0.009%，属计时边界抖动，非结构性不足）。
   E197 复验门评估：n=15≥15、超时率 6.7%≤10% 达标、无相反证据 → 晋升候选 PASS；
   p95×1.2≈53.7s 会击穿 P-14 约束（14+53.7=67.7>59s）→ 维持 [P-13]=45s 值不变。
   附录 A E232（bench:B-20260824-05）。
6. **[P-13] 晋升定稿（E233）**：owner 签认 E232 数据包（复验门⑤）——[P-13] 45s 由
   provisional@2026-08-24 转 **定稿**（值不变）；§5 状态更新 + 约束注记同步
   （P-13 与 P-14/P-15 同列定稿，调值需附录 A 登记）；代码侧无值变更
   （`params.ts deepReportBudgetMs=45_000` 已符合，C8 引用生效）。附录 A E233（bench:B-20260824-05）。
7. **v1.0 全量验收标准补全（E234）**：§5 注册表 P-09（v1.0 全量验收标准）tombstone 并入
   P-10（同 E197 P-01 先例）；P-10 落值转 conditional provisional@2026-08-24——验收条件集 =
   S1-S8 全功能切片落地且回归绿（§4.4 里程碑表 v1.0 范围，E220-E227）+ 既有验收口径回归
   （P-07/P-12/P-08）+ 成熟度 §12.4 达 L2+ + doc-lint 0 FAIL 与全量测试绿 + 附录 C 无相反
   证据与 owner 签认；S1-S8 收口后验收口径不再悬空。附录 A E234（bench:na(new-param)）。
8. **历史计划文档状态收口（E235）**：批量同步 30 份已完成但状态未同步的计划文档——
   v1.0 S2-S8（E221-E227）、架构审计全部批次（60b419d/615bb62/109021b/0cbde9b/ce1abf8/f575040/3fb4cc6）、
   历史计划 E144/E165-E168/E172/E184/E200-201/E204/E228/E229/E231 补提交记录；
   `2026-08-19-spreadsheet-formats.md` 结果段「待补」补全。附录 A E235（bench:na(new-param)）。

## 明日继续（按优先级）

1. **Tavily 9 月复核（备忘）**：9 月重置后跑 `npm run tavily:smoke`，确认远端 `/usage` 归零、
   本地计数重建，核对 [P-64] 计费口径（news/advanced 多倍计费）。
2. **附录 A 行数预算**：E228-E233 各 1 行，当前 531/950 余量充足；后续按需 retention 压缩。
3. **OCR 残差 / 常规项**：已收口（E201 词典 + E205 网格补位），方向 3（超分）维持暂缓。
4. **计划文档纪律**：后续每份计划文档当日收口状态与提交记录（本次 E235 已清理存量）；v1.0 收口验收照旧：：真实协议接入（S3 真实 MCP server、S5 平台适配器、S6 真实推送、S7 可执行 handler）后按 P-10 验收条件集跑全量验收，并按 E197 复验门评估 P-10 转定稿。



