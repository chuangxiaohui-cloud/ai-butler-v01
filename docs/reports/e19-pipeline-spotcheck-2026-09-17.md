# E19 管道抽检报告（E417 后）

> 日期：2026-09-17 · 分支：v0.2b · 计划：[`docs/plans/2026-09-17-e19-pipeline-spotcheck.md`](../plans/2026-09-17-e19-pipeline-spotcheck.md)

## 结论

**通过。** `Tauri 框架 架构 技术栈` 经完整 `npm run dev` 管道：

| 项 | 结果 |
|----|------|
| `gate_triggered` | `none`（非 must_clarify） |
| `confidence` | 0.954 |
| `evidence[]` | 3 |
| `answer` | 非空（≈1186 字），含架构三层 + 技术栈 + 安全模型 |
| 澄清误触 | 无（无「没把握/再说具体」） |

## 命令

```bash
npm run build
npm run dev -- "Tauri 框架 架构 技术栈"
```

原始 stdout/stderr：`tmp/e19-stdout.json`、`tmp/e19-stderr.txt`（本地，git 忽略）。

## 与复跑当时对比

| 时点 | 现象 |
|------|------|
| 条件② 复跑（修前） | `must_clarify`，conf=0，evidence=0 |
| E417 路由单测 | `direct` / `web_search` / `qa` |
| 本轮管道 | `gate=none`，有答有证 |

## 人工分（C.2 · 2026-09-17 抽检答案）

> 标尺：0=完全无用 / 1=部分可用 / 2=可用但有缺 / 3=完全满足  
> 综合分 = (相关性 + 时效 + 可用) / 3

| 维 | 分 | 理由 |
|----|----|------|
| 相关性 | **3** | 直接覆盖问句三要素：框架定位、三层架构（Rust/WebView/IPC）、技术栈与安全模型，切题完整 |
| 时效 | **2** | 引用官方 v2 安全页，且诚实标明版本证据冲突、不下伪结论；但证据夹杂过期 CSDN，版本号未定论 → 有缺 |
| 可用 | **3** | 结构可直接用于选型/讲解；体积/安全要点可落地；无空话澄清 |
| **综合** | **2.67** | (3+2+3)/3 |

**[P-12] 相关性栏（v02a-scores 口径）**：**3** · `hardAnswer=false`  
（条件② 全量报告里 E19 仍是修前 `must_clarify` 文本；本分绑定**本轮抽检答案**。全量 `bench:v02a` 重跑后请用新答案覆盖报表行。）

若你要改分，直接说「E19 改成 x/y/z」即可。
