# v0.2b MVP 实施规划（草稿，待启用）

> 状态：**草稿，未启用**｜编制：2026-08-12｜触发条件已满足：v0.2a `[P-12]` 30/31 通过并打 tag `v0.2a`
> 基准：v0.2a 实施规划 + 需求文档 v2.5 §4.4 / §8.1 / §8.2 / §12.3
> 前置依赖：MemoryCore（TencentDB-Agent-Memory）源码与本地 sidecar 启动
> 实施记录：2026-08-12 WP0 完成（MemoryCore sidecar 启动成功，端口 8420，`/health` ok；LLM=DeepSeek，embedding=none，BM25 zh；配置 `configs/tdai-gateway.local.yaml`）

---

## 0. 结论置顶（TL;DR）

1. **目标**：L2 记忆蒸馏（memory-core sidecar）+ MemoryCoreStore 切换 + ExperienceManager + Skill 生命周期治理，v0.1/v0.2a 记忆数据零丢失迁移。
2. **验收**：历史 L0/L1 数据零丢失自动迁移 + 31 条回归 + 蒸馏 worker 冒烟。
3. **记忆**：接口保持 §8.4 `MemoryStore` 不变，切换 = 配置项（SqliteDirectStore → MemoryCoreStore）。
4. **蒸馏**：异步后台 worker，失败不阻塞主对话，LLM 走 DeepSeek（[P-43] 30s 超时）。
5. **外部依赖**：MemoryCore 本地 HTTP sidecar（端口 8420，无 Docker）；当前工作区未发现源码，需先解决获取方式。

## 1. 目标与验收

| 项 | 定义 |
|----|------|
| 目标 | L2 蒸馏落地 + MemoryCoreStore + ExperienceManager + Skill 生命周期 |
| 验收标准 | 历史数据零丢失迁移；[P-12] 31 条回归通过；蒸馏失败不阻塞主对话 |
| 对应规格 | §8.1.4 sidecar / §8.4 MemoryStore / §8.2 Skill 生命周期 / §12.3 ExperienceManager |

## 2. 范围

### 2.1 做

| # | 交付物 | 对应规格 |
|---|--------|---------|
| 1 | MemoryCore sidecar 启动与健康检查 | §8.1.4（端口 8420） |
| 2 | v0.1/v0.2a SQLite → MemoryCore 数据迁移 | §8.4 schema v1 兼容 |
| 3 | MemoryCoreStore（同接口同 schema，配置切换） | §8.4 |
| 4 | L2 蒸馏 worker（异步，[P-43]） | §8.1.4 异步约束 |
| 5 | ExperienceManager（语义检索 + 置信度演化 + 衰减） | §12.3 |
| 6 | Skill 生命周期治理（冷存/衰减/降权/复审） | §8.2 |

### 2.2 不做

- ❌ MCP 子 Agent / 证据链 UI / 三栏 UI——v1.0
- ❌ CodeGraph 索引资产——P2 后续按需

## 3. WBS（草稿）

| WP | 任务 | 依赖 | 验收 |
|----|------|------|------|
| WP0 | MemoryCore 获取 + sidecar 启动 + 健康检查 | 源码到位 | `GET /health` OK，端口 8420 |
| WP1 | 数据迁移：l0/l1 → MemoryCore | WP0 | 零丢失 + 行数/哈希校验 |
| WP2 | MemoryCoreStore 实现与配置切换 | WP1 | 同接口同 schema，回归测试 |
| WP3 | L2 蒸馏 worker（异步，[P-43] 30s） | WP2 | 失败降级保留 L0，不阻塞主对话 |
| WP4 | ExperienceManager（embedding 或 BM25 兜底） | WP2 | 语义检索 + 置信度演化单测 |
| WP5 | Skill 生命周期治理（冷存/衰减/降权） | WP4 | 四字段 + 治理规则单测 |
| WP6 | 验收：历史迁移 + 31 条回归 + 蒸馏冒烟 | WP1-5 | 全部通过 |

## 4. 关键参数

| 场景 | 引用 |
|------|------|
| 蒸馏 worker LLM 超时 | [P-43] |
| memory-core RAM 上限 | [P-59] |
| SDK 调用超时 | [P-42] |
| Skill 冷存天数 | [P-31] |
| Skill 衰减系数 | [P-30] |
| 语义匹配置信度下限 | [P-32] |
| 👎 降权触发阈值 | [P-79] |

## 5. 风险与对策

| 风险 | 等级 | 对策 |
|------|------|------|
| MemoryCore 源码未就位 | 🔴 | 决策点：git clone / 用户提供路径；未就位前 WP0 阻塞 |
| 无本地 embedding | 🟡 | `embedding.provider: none`，检索用 BM25/FTS5 兜底，语义检索后置 |
| 蒸馏成本/超时 | 🟡 | 异步 worker + 失败降级；DeepSeek 轻模型，[P-43] 超时 |
| 迁移破坏历史数据 | 🔴 | 先备份 SQLite，行数/哈希校验，可回滚 |

## 6. 待拍板决策点

1. **MemoryCore 源码来源**：工作区未发现；需要 git clone 或你提供本地路径。
2. **蒸馏触发**：按 v0.1 决策④——用户 👍 后提炼 L1；L2 后台批处理，建议沿用。
3. **embedding 退化**：无向量时 BM25/FTS5 兜底；语义 embedding 是否引入外部模型待定。

---

*本文档为 v0.2b 草稿，未启用；一切规格冲突以 `一人公司AI-Agent需求文档_v2.5.md` 为准。*
