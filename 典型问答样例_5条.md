# 典型问答样例（5 条）

> 用途：校准审计方对 `docs/audit-package-checklist.md` §6.1「人工判定可用」的理解。
> 样例均来自本项目真实问答历史（`data/trajectory.jsonl` / 计划文档 / 每日交接），非占位示例。
> 「可用」≠「有输出」，须同时满足以下全部条件：
> 1. 无报错退出（exit 0，无未处理异常）
> 2. 响应时间在对应场景 §5 预算内（合成链 [P-116] 18000ms；github 调研类 skill 长文 [P-122] 90000ms）
> 3. 输出包含 answer 契约四字段：`answer / confidence / evidence（来源引用）/ gate_triggered`
> 4. 信息准确、可操作、无幻觉；缺失维度诚实标注「未获取」，不编造
> 5. Token 消耗在合理范围（对照 `data/usage.jsonl` 基线 ±30%）
> 「可用/不可用」判定依据（≤50 字/条）须写入审计报告附录，锚定 v2.5 需求 §4.2/§6.1.3/§6.5/§6.6（见 checklist §6.1 注）。

---

## 样例 1：工程类 - 开源项目调研（github 解读）

**用户输入**：
> `https://github.com/deepseek-ai/deepseek-harness 这项目是做什么用的？`
> 来源：2026-08-29 用户 9 轮真实复测（E274-E284），历史过程见 `docs/2026-08-29-progress-handoff.md`

**期望输出要素**：
- [ ] 一句话结论带限定词（如「推测是…」），非断言
- [ ] 定位 / 技术栈 / 使用 / 活跃度 / 风险 五维覆盖
- [ ] evidence ≥3 条（README / package.json / GitHub API），来源可点击
- [ ] confidence 与内容匹配；缺失维度标注「未获取」
- [ ] 响应在预算内（github-reader 合成按 [P-122] 90s；实测 synthesisMs 8.0s、totalMs 18.3s）

**历史参考输出**（08-29 末轮，供校准，正式判定以审计方复跑为准）：
> 定位=DeepSeek 开源的 agent harness（「一切皆插件」+ Cordis，developer preview）；使用=`npx @deepseek-ai/dsh web`；风险=破坏性变更明确、不建议生产采用；confidence 0.65、evidence 6 条、synthesisMs 8.0s、totalMs 18.3s。

**实际输出**（审计方复跑后填写）：
> [留空]

**可用判定**：☐ 可用 ☐ 不可用
**判定依据**（≤50 字）：[留空]

---

## 样例 2：工程类 - 性能基准 / 数值查询

**用户输入**：
> `延迟最低的数据库有哪些`
> 来源：2026-08-29 用户实测（E278：连续 3 次 synthesis_timeout 91s/97s/104s 触发修复；E279 数值增强）

**期望输出要素**：
- [ ] 答案含具体数值（延迟 ms 级 + 单位），非「有哪些」无数据泛答
- [ ] 证据含性能基准来源；多库对比时给双方数值
- [ ] 无横向对比数据时诚实标注「无法断定最低」，不硬排
- [ ] 标注 confidence（如「中-基于单库基准，无同场对比」）
- [ ] 响应在预算内（CLI 主链路 [P-116] 18000ms；实测 E278 修复后全链路 49s）

**历史参考输出**（E278 修复后真实 CLI 重跑，供校准）：
> gate=none、elapsed 49s（medium v4-flash）；答案含 Redis / MongoDB(内存模式) / TiDB / MySQL / KVStore / OceanBase，并诚实标注「无横向对比、无法断定最低」。

**实际输出**（审计方复跑后填写）：
> [留空]

**可用判定**：☐ 可用 ☐ 不可用
**判定依据**（≤50 字）：[留空]

---

## 样例 3：工程类 - 数值排名（已知失败案例，修复后复测）

**用户输入**：
> `中国AI大模型公司市值较高的是哪几家？`
> 来源：2026-08-27/28 用户实测（E275 失败定位 → E277/E279 机制修复），过程见交接

**期望输出要素**：
- [ ] 答案含具体市值数值（金额 + 单位），非「哪几家」无数据泛答
- [ ] evidence 含数据来源（财报 / 权威榜单），数值可核对
- [ ] confidence 按核心维度完整度计算；缺数值维度时诚实标注
- [ ] 响应在预算内（CLI 主链路 [P-116] 18000ms）

**历史参考输出**（修复前，供校准）：
> 召回无问题（候选池含智谱 5000 亿港元 / 寒武纪等），但答案缺具体市值数值 → **不可用**；E277/E279 已修复（P-137 领域中性后缀 + numeric-pattern 五类单位 + judge 数值证据要求），单测钉死，e2e 待复测。

**实际输出**（审计方复跑后填写）：
> [留空]

**可用判定**：☐ 可用 ☐ 不可用
**判定依据**（≤50 字）：[留空]

---

## 样例 4：生活类 - 信息检索（天气）

**用户输入**：
> `华强北 天气`
> 来源：真实历史查询（2026-08-16，`data/trajectory.jsonl`）

**期望输出要素**：
- [ ] 给出当地当前/近期天气（温度 + 天气状况 + 时间）
- [ ] 信息来源为可靠天气源，标注信息时效
- [ ] 标注 confidence（如「中-基于天气源实时数据，未实地验证」）
- [ ] 响应在预算内（CLI 主链路 [P-116] 18000ms）

**实际输出**（审计方复跑后填写）：
> [留空]

**可用判定**：☐ 可用 ☐ 不可用
**判定依据**（≤50 字）：[留空]

---

## 样例 5：工程类 - 器件选型推荐（决策辅助形态）

**用户输入**：
> `电源芯片 推荐`
> 来源：真实历史查询（2026-08-19，`data/trajectory.jsonl`）

**期望输出要素**：
- [ ] 推荐 ≤3 款具体型号，含关键参数对比（输入电压 / 输出电流 / 效率 / 封装）
- [ ] 说明推荐理由（如「TPS5430 适合 24V 输入降压」）
- [ ] 引用器件数据手册 / 评测作为 evidence
- [ ] 标注 confidence；提示「以官方数据手册为准」
- [ ] 响应在预算内（CLI 主链路 [P-116] 18000ms）

**实际输出**（审计方复跑后填写）：
> [留空]

**可用判定**：☐ 可用 ☐ 不可用
**判定依据**（≤50 字）：[留空]

---

## 附注（供审计方扩展复跑）

- 其他真实历史 query 可替换/补充复跑：`KiCad vs Altium Designer 对比 2024`（技术选型）、`BUCK电路 电感发烫 原因`（调试辅助）、`Redis 和 Memcached 哪个读取延迟更低`、`PostgreSQL 16 写入性能 benchmark`（数值对比，设计已钉死，e2e 待复测）。
- 样例 1/3 的「历史参考输出」仅用于校准审计方标准；正式可用判定一律以审计方复跑结果为准。
- 样例 1 预算按 github-reader skill 档（[P-122]）；样例 2-5 按 CLI 主链路（[P-116]）。
- 判定依据须 ≤50 字/条，并对照 v2.5 需求 §4.2（answer 契约）+ §6.5 规则① fact_consistency + §6.6 规则② 置信度门控 + §6.1.3 规则③ 关键词兜底（降级可见）。