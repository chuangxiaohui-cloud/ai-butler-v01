# v0.1 MVP 实施规划

> 基准：v2.5 定稿（tag `v2.5`，commit `5d6c6c5`）｜编制：2026-08-12｜状态：**已拍板，待 WP0 开工**
> 对齐：§4.4 MVP 切片 / §6.8 参数落地顺序 / §13 代码目录 / 附录 C 基准数据

---

## 0. 结论置顶（TL;DR）

1. **v0.1 目标**：跑通"搜索问答"最小闭环——10 条基准 query 走完 Stage 1-6 管道，Bocha + AnySearch 常开，无 Tavily。验收标准 [P-07]（10 条中 ≥[P-07] 相关性达标且无 0 分硬答）。
2. **范围**：只做 §6.8 步 0-2 + 步 4（规则③安全阀独立部署）；**不做** Tavily / L2 蒸馏 / MCP 子 Agent / 证据链 UI / 全量安全模型。
3. **安全最小集** = §10.1 文件沙箱根目录白名单（唯一例外不做）。
4. **记忆**：L0/L1，SqliteDirectStore 直连（better-sqlite3），schema = §8.4 schema v1，v0.1 发布即冻结。
5. **冷启动三来源**（§12.1）前置：预置 Skill 库 + Wiki 种子 + 人格提示，随安装打包。
6. **技术栈**（§9.1）：Tauri + Node/TS + express + better-sqlite3，无 Docker。
7. **决策确认（2026-08-12 老张审核通过）**：① 验收 10 条用 §5.2 推荐版 ② API Key 用 `.env` 本地文件（补 `.env.example`）③ 纯 CLI 先行（输出结构化 JSON）④ Stage 6 写 L0（JSONL），👍 后提炼 L1 ⑤ 预置 Skill 先做 2 项核心 + 4 项占位（列清单）。

---

## 1. 目标与验收

| 项 | 定义 |
|----|------|
| 目标 | 10 条基准 query 跑通搜索管道（Bocha+AnySearch 常开，无 Tavily） |
| 验收标准 | [P-07]：10 条中 ≥8 条相关性 ≥2 分且无 0 分硬答 |
| 记忆要求 | L0/L1，存储 schema = §8.4 schema v1（SqliteDirectStore） |
| 对应 §6.8 | 步 0-2 + 步 4（规则③安全阀独立部署） |
| 冷启动 | 三来源前置（§12.1），v0.1 发布即生效 |
| 成熟度联动 | 验收通过 ≈ L0→L1（§12.4 等级定义） |

## 2. 范围界定

### 2.1 做（v0.1 交付物）

| # | 交付物 | 对应规格 |
|---|--------|---------|
| 1 | Stage 1-6 搜索管道编排 | §6.0 管道总览 + §6.3 接口契约 `answer(query) → { answer, confidence, evidence[], gate_triggered }` |
| 2 | Bocha + AnySearch 适配器 | §6.2 引擎名单（冻结版，v0.1 只启用两路常开） |
| 3 | 4 串行过滤器 + 加权评分 | §6.5.1 + §6.5.2（对应 §6.8 步 1） |
| 4 | 规则① fact_consistency | §6.5.5（对应 §6.8 步 2） |
| 5 | 规则③ 关键词硬规则兜底 | §6.1.3（对应 §6.8 步 4，安全阀独立部署） |
| 6 | 来源权威注入 | §6.5.3（域名权威度表 + 原厂域名映射） |
| 7 | 意图分类 + Query 构造 | §6.1.2（8 意图枚举 + 构造策略表） |
| 8 | 预处理（黑话/脱敏/缓存/澄清） | §6.1.1 + §6.1.4 |
| 9 | SqliteDirectStore + schema v1 | §8.4（L0/L1） |
| 10 | 文件沙箱根目录白名单 | §10.1（安全最小集） |
| 11 | 冷启动三来源 | §12.1 + §12.2 预置 Skill 6 项 |

### 2.2 不做（明确非目标）

- ❌ Tavily（含条件并联 / AI Answer / warmup）——v0.2a 才上
- ❌ L2 蒸馏（memory-core sidecar）——v0.2b 才上
- ❌ MCP 子 Agent / 执行门 / 项目协作——v1.0
- ❌ 证据链 UI / 深度报告 UI——v1.0
- ❌ 全量安全模型（命令白名单 / 脱敏 / 安全 TDD 用例表）——v1.0（仅保留 §10.1 沙箱）
- ❌ ExperienceManager / Skill 生命周期治理——v0.2b 配套
- ❌ 上下文压缩 / 逐字窗口——随 L1 记忆基础版即可，[P-29] 完整规则后置

## 3. 技术栈与项目骨架

### 3.1 技术选型（§9.1 已定，不重议）

| 层 | 选型 | 理由 |
|----|------|------|
| 桌面壳 | Tauri v2 | 轻量（对比 Electron），WebView 沙箱 |
| 主进程 | Node.js / TypeScript | 全栈 TS 统一 |
| Gateway | 自研 ~670 行 | 2 个 npm 依赖：express + better-sqlite3，无 Docker |
| 记忆存储 | better-sqlite3（SqliteDirectStore） | v0.2b 换 MemoryCoreStore 时同接口同 schema，切换 = 配置项 |
| 前端 | React | Tauri 标配，证据链 UI 后置不影响 |
| LLM | 云端主备双供应商 | §1 已定，轻任务小模型档 / 重任务大模型档 |

### 3.2 目录结构（对齐 §13 代码目录，实施期补全）

```
E:\WorkBuddy_WorkSpace\
├── src/
│   ├── main.ts                 # 入口（CLI 模式 / Tauri 模式二选一）
│   ├── search/
│   │   ├── pipeline.ts         # Stage 1-6 编排（§6.0）
│   │   ├── stages/
│   │   │   ├── s1_prepare.ts   # Stage 1 预处理（≤[P-20]）
│   │   │   ├── s2_classify.ts  # Stage 2 意图分类 + Query（≤[P-04]）
│   │   │   ├── s3_search.ts    # Stage 3 搜索执行（≤[P-02]）
│   │   │   ├── s4_fusion.ts    # Stage 4 过滤 + 评分（≤[P-05]）
│   │   │   ├── s5_synthesize.ts# Stage 5 秘书合成
│   │   │   └── s6_post.ts      # Stage 6 后处理 + 记忆写入（≤[P-21]）
│   │   ├── providers/
│   │   │   ├── bocha.ts        # Bocha 适配（[P-63] 配额检查）
│   │   │   ├── anysearch.ts    # AnySearch 适配（[P-65] 配额检查）
│   │   │   └── types.ts        # 引擎统一返回类型
│   │   ├── fusion.ts           # 4 串行过滤器 + 加权评分（§6.5.1/6.5.2）
│   │   ├── rule1.ts            # 规则① fact_consistency（§6.5.5）
│   │   ├── rule3.ts            # 规则③ 关键词硬规则兜底（§6.1.3，独立部署）
│   │   ├── authority.ts        # 来源权威注入（§6.5.3）
│   │   └── cache.ts            # 结果缓存（[P-61]/[P-62] TTL）
│   ├── memory/
│   │   ├── store.ts            # MemoryStore 接口 + SqliteDirectStore（§8.4）
│   │   └── schema.sql          # schema v1（冻结）
│   ├── skills/                 # 预置 Skill 6 项（§12.2）
│   │   ├── chip-analysis/      # 芯片/器件分析
│   │   ├── github-reader/      # GitHub 项目解读
│   │   ├── jargon-map/         # 黑话映射
│   │   ├── datasheet-speed/    # Datasheet 速读
│   │   ├── circuit-topology/   # 常见电路拓扑
│   │   └── industry-kits/      # 行业知识包（占位）
│   └── wiki/                   # 预置 Wiki 种子（器件踩坑经验库）
├── bench/
│   ├── raw_scores.csv          # 93 条人工打分（已入库，SHA-256 见附录 C.4）
│   └── v01-queries.json        # v0.1 验收 10 条（待拍板名单）
├── scripts/
│   ├── doc-lint.ts             # 文档宪法执法（已有）
│   └── gen-queries.ts          # 从 raw_scores.csv 抽验收 query
├── .env.example                # API Key 模板（不进 git）
└── 一人公司AI-Agent需求文档_v2.5.md
```

## 4. 工作包分解（WBS）

> 串行依赖：WP0 → WP1 → WP2 → WP3 → WP4 → WP5 → WP6 → WP7 → WP8（WP9 全程并行）

| WP | 任务 | 对应规格 | 依赖 | 验收 |
|----|------|---------|------|------|
| **WP0** | 工程初始化：git 分支 + TS 脚手架 + 目录骨架 + `.env.example` + `.gitignore` 排除 `.env` / `node_modules` | §13 | 无 | `npm run dev -- --help` 可跑；`.env.example` 含 Key 占位说明 |
| **WP1** | 验收 query 集：从 raw_scores.csv 抽 10 条 + 生成 v01-queries.json | 附录 C.3 + [P-07] | WP0 | 10 条覆盖 4 类意图（见 §5） |
| **WP2** | 步 0 标定集验证：确认 5 处 bug 已修复 + 极端值清洗，SHA-256 复核。**启动时补全**：评分脚本 `scripts/aggregate-scores.ts` 函数级定位（§6.4 权威定义，5 处 bug 明细见附录C.1）+ 修复前后输出 diff 示例 | §6.4 + 附录 C.4 | WP1 | 干净标定集可用 |
| **WP3** | Stage 1 预处理 + Stage 2 意图分类（8 意图 + Query 构造 + 澄清中断） | §6.1.1/6.1.2/6.1.4 | WP2 | 单测：10 条 query 意图分类准确率 ≥80% |
| **WP4** | Stage 3 搜索执行：Bocha + AnySearch 适配器 + 配额检查 + 超时降级 | §6.2 + [P-63]/[P-65]/[P-02]/[P-03] | WP3 | 10 条 query 两引擎均返回结果，超时降级路径单测 |
| **WP5** | 规则③ 安全阀独立部署（独立于分类器） | §6.1.3（步 4） | WP3 单测通过（10 条意图分类准确率 ≥80%） | 单测：S02/L05 命中关键词 → 强制严肃通道 |
| **WP6** | Stage 4：4 串行过滤器 + 加权评分 + 规则① fact_consistency + 来源权威注入 | §6.5（步 1 + 步 2） | WP4 | 单测：E02 TPS5430 数值冲突仲裁（官方源 4.5-36V 胜） |
| **WP7** | Stage 5 秘书合成 + Stage 6 后处理 + 记忆写入（L0/L1） | §6.3 接口契约 | WP6 | answer() 四字段完整返回 |
| **WP8** | 记忆：SqliteDirectStore + schema v1（建表 + put/recall/forget），**L0 统一 JSONL 格式** | §8.4 | WP0 | schema v1 建表单测；put→recall 往返；L0 写出 JSONL |
| **WP9** | 冷启动三来源：预置 Skill **2 项核心 + 4 项占位**，Wiki 种子 + 人格提示；占位项列清单防遗忘 | §12.1/12.2 | 并行 | chip-analysis/jargon-map 可装备；占位项清单写入 `skills/README.md` |
| **WP10** | 安全最小集：文件沙箱根目录白名单 | §10.1 | WP0 | 越界路径返回 403 + 审计日志 |
| **WP11** | v0.1 验收：10 条 query 全跑 + [P-07] 判定 + 回归报告 | [P-07] | WP3-10 全部 | 10 条中 ≥[P-07] 达标且无 0 分硬答 |

## 5. 验收 query 名单（决策点①，待老张拍板）

### 5.1 选择策略

从附录 C.3 的 31 条中抽 10 条，要求：
1. **意图覆盖**：factual ×2 / experience ×2 / comparison ×1 / how_to ×1 / troubleshooting ×2 / 严肃(规则③验证) ×2
2. **引擎覆盖**：Bocha 赢局（E02）、AnySearch 赢局（E04/E06）都要有
3. **规则③验证**：至少 2 条严肃类（S02 高血压 / L05 个税）
4. **诚实边界**：E16（Tavily 独家赢局）作为"无 Tavily 时的降级用例"——预期不硬答

### 5.2 推荐名单（10 条，已确认）

| # | ID | Query | 意图 | 验证点 |
|---|----|-------|------|--------|
| 1 | E01 | STM32F103C8T6 最大主频是多少 | factual | 实体精确匹配过滤器 |
| 2 | E02 | TPS5430 输入电压范围 | factual | 规则① 数值冲突仲裁（Bocha 赢局） |
| 3 | E04 | STM32F103 ADC 多通道采集 踩坑 经验 | experience | 经验类 Query 构造 + 时效过滤器 |
| 4 | E06 | MOSFET 栅极驱动电阻 选型 注意事项 | experience | AnySearch 赢局（3/3/3） |
| 5 | E08 | KiCad 和 Altium Designer 对比 优缺点 | comparison | 对比意图权重 [P-23] |
| 6 | E11 | Keil MDK JTAG 配置 步骤 | how_to | how_to 意图 + usability 权重 |
| 7 | E14 | STM32 HAL_UART_Transmit 阻塞 卡死 | troubleshooting | Bocha 赢局（2.67） |
| 8 | S02 | 高血压 用药注意事项 禁忌 | 严肃 | 规则③ 命中"药品名"→严肃通道（AnySearch 赢局） |
| 9 | L05 | 个人所得税 专项附加扣除 怎么申报 | 严肃 | 规则③ 命中"税率/法规"→严肃通道 |
| 10 | E16 | ESP32 I2C 通信失败 无应答 | troubleshooting | **诚实边界**：Tavily 独家赢局，无 Tavily 时必须低置信标注/不硬答 |

> **E16 特别说明**：附录 C.3 中 E16 的 Bocha=0、AnySearch=2.33、Tavily=3。v0.1 无 Tavily，预期 AnySearch 兜底给出部分可用答案；若融合分低于门控，则触发"低置信标注"而非硬答——这正是 [P-07]"无 0 分硬答"的验证点。

## 6. 关键参数引用（数值单家，全部引 §5）

| 场景 | 引用 |
|------|------|
| 验收通过率 | [P-07] |
| Stage 预算（300ms/500ms/5s/1s/1s） | [P-20]/[P-04]/[P-02]/[P-05]/[P-21] |
| 引擎超时 | [P-03] AnySearch 5s /（Tavily [P-35] 后置） |
| 配额检查 | [P-63] Bocha 日配额 / [P-65] AnySearch 日配额 |
| 缓存 TTL | [P-61] factual 7 天 / [P-62] experience 30 天 |
| 评分权重 | [P-22] relevance / [P-24] fact_consistency / [P-23] 意图动态权重 |
| 官方源乘数 | [P-18] ×1.2 |
| 用户反馈调权 | [P-72] 升 / [P-19] 降 / 上下限 [P-73]/[P-74] |
| 大文件分页 | [P-70]（Datasheet 后置，v0.1 不涉及） |

## 7. 验收流程

1. `npm run bench:v01` → 跑 10 条 query，输出每条 `{ query, answer, confidence, evidence[], gate_triggered }`
2. 人工复核（老张）：按附录 C.2 评分细则打相关性分（0-3）
3. [P-07] 判定：达标数 ≥ 阈值 且 无 0 分硬答
4. 产出 `bench/v01-report.md`：逐条记录 + 聚合 + 遗留问题清单
5. 通过 → 打 tag `v0.1`，schema v1 冻结，进入 v0.2a 规划

> **时间缓冲纪律**：v0.1 无日历排期（WBS 为依赖图非时间表）。若后续引入排期缓冲，缓冲仅用于吸收 WP2-7 延期；**未被消耗的缓冲自动转为 WP11 回归测试时间，不得挪作新功能开发**（防缓冲隐性侵蚀）。

## 8. 风险与对策

| 风险 | 等级 | 对策 |
|------|------|------|
| Bocha 在工程类 0 分集中（附录C） | 🟡 | 步 6 评分后过滤，低置信标注不硬答；AnySearch 兜底 |
| API Key 泄露（老张敏感项） | 🔴 | `.env` 本地文件（已确认）；补 `.env.example` 模板；`.gitignore` 排除 `.env`；脱敏规则 §10.3 提前内置到 Stage 1 |
| E16 无 Tavily 预期降级 → 影响通过率 | 🟡 | 名单已含 2 条高分兜底（E06/E14），[P-07] 有容错空间 |
| 意图分类 8 类在 10 条样本上过拟合 | 🟡 | 单测阈值 ≥80% 仅为冒烟线；真实标定在 v0.2a 31 条 |
| 10 条抽样偏差 | 🟡 | 名单覆盖 4 类意图 × 5 分类，且 2 条严肃验证规则③ |
| LLM 双供应商任一超时 | 🟢 | Stage 5 超时降级（[P-06] 8s 预算 + 备用供应商切换） |

## 9. 决策确认（老张 2026-08-12 审核结论）

| # | 决策点 | 审核结论 | 修改建议 | 本规划落点 |
|---|--------|---------|---------|-----------|
| ① | 验收 10 条名单 | ✅ 通过 | 替换需记录理由 | §5.2 名单写死；后续替换必须在 `bench/v01-queries.json` 注释中写明理由 |
| ② | API Key 存放 | ✅ 通过 | 补 `.env.example` | WP0 创建 `.env.example`；`.gitignore` 排除 `.env` |
| ③ | 纯 CLI 先行 | ✅ 强烈通过 | 确保输出为结构化 JSON | WP7 输出契约严格 `answer(query) → { answer, confidence, evidence[], gate_triggered }`；CLI 以 JSON 行输出 |
| ④ | Stage 6 写 L0，👍 后提炼 L1 | ✅ 通过 | 明确 L0 存储格式为 JSONL | WP8 L0 表字段 `raw_jsonl TEXT`，每次问答追加一行 JSON；L1 提炼后写入 `l1_memory` 表 |
| ⑤ | 2 项核心 + 4 项占位 | ✅ 通过 | 占位项列清单防遗忘 | WP9 `skills/README.md` 列 6 项清单，核心 2 项实现、占位 4 项标 `[PLACEHOLDER]` |

### 补充执行细节

1. **`.env.example` 模板**（WP0）：
   ```bash
   # v0.1 仅 Bocha + AnySearch；Tavily 在 v0.2a 启用
   BOCHA_API_KEY=your_bocha_key_here
   ANYSEARCH_API_KEY=your_anysearch_key_here
   # LLM 主备（OpenAI 兼容协议）
   LLM_PRIMARY_BASE_URL=https://api.provider1.com/v1
   LLM_PRIMARY_API_KEY=sk-xxx
   LLM_FALLBACK_BASE_URL=https://api.provider2.com/v1
   LLM_FALLBACK_API_KEY=sk-yyy
   ```

2. **CLI 输出 JSON 结构**（WP7）：
   ```json
   {
     "query": "原始问题",
     "answer": "秘书合成答案",
     "confidence": 0.82,
     "evidence": [
       {"title": "...", "url": "...", "domain": "...", "score": 2.8, "type": "[hard]"}
     ],
     "gate_triggered": "none",
     "elapsed_ms": 4200
   }
   ```

3. **L0 JSONL 格式**（WP8）：每行一个对象，字段：`session_id`, `query`, `answer`, `confidence`, `evidence_hash`, `timestamp`。

4. **占位 Skill 清单**（WP9）：github-reader / datasheet-speed / circuit-topology / industry-kits 四项先建空目录 + `README.md` 说明占位理由与预计启用版本。

---

## 附：v0.1 完成后自然衔接

| 后续版本 | 内容 | 触发条件 |
|---------|------|---------|
| v0.2a | 31 条全量 + 三规则全量 + Tavily 条件并联 + 4 过滤器正式阈值 | [P-07] 通过后 |
| v0.2b | L2 蒸馏（memory-core sidecar）+ ExperienceManager + Skill 生命周期 | v0.2a 通过后 |
| v1.0 | MCP 子 Agent + 证据链 UI + 全量安全模型 + 三栏布局 | §4.4 全功能 |

---

*本文档为 v0.1 实施规划，非需求文档正本；一切规格冲突以 `一人公司AI-Agent需求文档_v2.5.md` 为准。*
