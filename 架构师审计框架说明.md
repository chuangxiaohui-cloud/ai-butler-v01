# 架构师审计框架说明

> ⚠️ **已废弃**：本文档已被 `架构师审计框架说明 v2.0.md`（v2.0.1 修订）取代。
> 请以 v2.0 为准，勿再引用本文档（章节编号与 ¥447 口径均以 v2.0 为准）。


> 本文档定义本次审计的角色定位、思维模型与输出要求。
> 审计方请以【资深架构师】身份执行，而非 QA / 代码规范检查员。
> 配套：`docs/audit-package-checklist.md`（v2.5）为审计依据；本文档是执行框架，清单是检查项清单。

## 1. 角色定位

你不是在"找 bug"，而是在回答一个问题：

> **"以当前 ¥447 开发成本约束和 AI 辅助开发模式，这个架构能否支撑一人公司 AI-Agent v2.5 的核心场景稳定运行 6 个月以上？"**

**核心场景**（L1「用户感知的功能失效」的判定范围，锚定需求文档 v2.5）：
- 知识问答主链路：§6 搜索问答管道（意图分类 → 检索 → 融合 → 合成）
- 安全门：§10 五源信任域（命令/URL/动作白名单、沙箱、Prompt Injection / SSRF / 命令注入防护）
- 三工作模式：§4 知识问答 / 项目协作 / 生活助手

所有检查项都服务于这个问题。与这个问题无关的发现（如变量命名不规范、缺少 JSDoc），除非影响可维护性到"6 个月内必然导致重大故障"的程度，否则不列入报告。

## 2. 思维模型：三层过滤

对每个发现，依次过三层过滤器：

| 层级 | 问题 | 通过条件 |
|------|------|----------|
| L1 业务影响 | 这个问题会导致用户感知到的功能失效/数据错误/安全风险吗？（含 AI 特有安全：Prompt Injection / SSRF / 命令注入） | 是 → 进入 L2 |
| L2 修复成本 | 在当前代码结构和 AI 辅助模式下，修复成本是否 ≤ 3 人天？ | 是 → 进入 L3；否 → 标记为"架构债务"，给出分阶段方案 |
| L3 优先级 | 相比其他发现，这个问题的紧急程度排第几？ | TOP 10 → 写入报告正文；其余 → 附录 |

**未通过 L1 的发现**：不写入报告正文，可在附录"低优先级观察"中一句话带过。

## 3. AI 项目特异性认知

本项目代码由 Codex + DeepSeek 生成，具有以下特征，审计时需特别注意：

- **防御性过当**：AI 倾向到处加 try-catch + fallback，错误可能被静默吞掉
- **同义反复测试**：测试可能只断言"代码做了代码做的事"，未验证业务意图
- **过度抽象**：可能存在仅被调用一次的 interface/factory/strategy
- **注释漂移**：AI 改代码时常忘更新注释，注释可能与实现不一致
- **PARAM 脱节**：代码中的默认值/阈值可能与 PARAM 注册表声明不一致

以上特征已转化为具体检查项，请严格执行：PARAM 对齐见 Checklist §5.1；AI 代码特异性完整清单（含 mock 过度/集成盲区、依赖注入真实性，共 6 项）见 Checklist §7。

## 4. 输出要求

### 4.1 报告结构
- 执行摘要（≤1 页）：结论 + TOP 3 风险 + 修复建议优先级
- 问题清单表格：编号 / 模块 / 严重度 / 置信度 / 修复建议 / 预估工时 / 预估成本（对照 ¥447 预算）
- 详细分析（按模块组织）：证据 + 推理过程
- 附录：低优先级观察 / 工具限制说明 / 抽样清单 / LOW 置信度汇总

### 4.2 置信度标注（Checklist §9）
每项结论必须标注：
- **HIGH**：有自动化测试/静态分析/渗透测试证据
- **MEDIUM**：有人工代码审查 + 抽样验证
- **LOW**：仅基于文档/口头确认/有限观察

LOW 置信度项需在报告中单独汇总，注明验证局限与建议补验方式。

### 4.3 修复建议约束
- 除非有量化证据证明"6 个月内必然导致重大故障"，否则不提议整体重写/更换框架；如确需，给出分阶段迁移方案并对照 ¥447 预算评估
- 每条建议需包含：具体修改位置 + 修改内容 + 预期效果 + 预估工时
- 优先推荐"最小侵入式修复"（如加一行日志、改一个配置）
- 修复建议不得破坏回归基线：`npm run bench:devil-v25`（122 条）为验收口径；改动需保持全量单测 + 集成（32/32）+ doc-lint 0 FAIL 0 WARN

## 5. 必查项（架构层面，与仓库治理对齐）

- [ ] **核心契约一致性**：`answer(query) → { answer, confidence, evidence[], gate_triggered }` 在 CLI / gateway / UI 共用同一 pipeline；确认不存在第二套问答链路（AGENTS.md 硬约定）
- [ ] **安全防线**：§10 五源信任域落地（命令/URL/动作白名单、沙箱）；Prompt Injection / SSRF / 命令注入 测试覆盖
- [ ] **PARAM 单家**：§5 注册表与 `src/config/params.ts` 值级一致（doc-lint C8 + 人工抽验，Checklist §5.1）
- [ ] **降级可观测**：fallback/兜底路径有日志与 `gate_triggered` 透传，非静默吞错

## 6. 沟通原则

- 遇到不确定的判断，标 LOW 并说明原因，不要硬给结论
- 工具不可用时，降级为人工方法并注明，不要跳过检查项
- 发现材料缺失时，主动声明"未发现"，不要自行脑补
- T+3（T=审计启动日）中期检查时提交阶段性成果，不要等到最终报告才暴露问题